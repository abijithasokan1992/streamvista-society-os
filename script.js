document.addEventListener("DOMContentLoaded", () => {
  const banner = document.getElementById("deployBanner");
  if (banner) {
    banner.textContent = "Hosted via GitHub Pages — your app is live.";
  }
});
